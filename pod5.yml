apiVersion: v1
kind: Pod
metadata:
  name: raju
spec:
  containers:
  - name: c001
    image: ubuntu
    command: ["/bin/bash", "-c", "while true; do echo hello hanji kon; sleep 5; done"]
    volumeMounts:
    - name: testsecret
      mountPath: "/tmp/secret"

  volumes:
    - name: testsecret
      secret:
        secretName: mysecret
